/**
 * @ai-product-copilot/ui
 * Shared UI component library (shadcn/ui based)
 *
 * Components will be added here as they are built.
 * Re-export each component from this barrel file.
 */

// Example: export { Button } from "./button";
